export interface EInvoiceSignInValues {
    gstin: string;
    clientId: string;
    password: string;
}

export interface eInvoicingSign {
    id: string;
    title: string;
    description: string;
    icon: string;
}

export interface EInvoiceFeature {
    id: string;
    title: string;
    description: string;
    icon: string;
}
