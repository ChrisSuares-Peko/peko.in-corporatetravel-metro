/** Copy for the user "My transactions" view (cardholder's own charges). The table itself is API-backed. */

export const MY_TRANSACTIONS_COPY = {
    title: 'My transactions',
    subtitle: 'Every charge made on cards issued to you.',
    exportCsv: 'Export CSV',
    issuedBy: 'Issued by',
} as const;
